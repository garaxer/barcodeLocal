import { Theme as AugmentedTheme } from '@material-ui/core/styles';

export default (theme: AugmentedTheme) => ({
  root: {
    flexGrow: 1,
    marginTop: theme.spacing(2),
    // fontFamily: "'Heebo', sans-serif",
    // backgroundColor: '#f0f0f0',
  },
});
